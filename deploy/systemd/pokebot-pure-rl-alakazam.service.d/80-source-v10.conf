[Service]
# v10 differs from v9 only in checkpoint-contract accounting: architecture
# size comes from immutable state-dict tensors instead of stale train-time
# ``extra.param_count`` telemetry.
WorkingDirectory=/home/inzi/poke-bot-agent-deployments/pure-rl-resident-v10
