[Service]
# The currently active v9 process and immutable design fingerprint were born
# from resident-v2. Until an explicit clean-boundary migration is committed,
# a spontaneous systemd restart must resume that exact source tree instead of
# entering a source-drift failure loop. Installing/reloading this drop-in does
# not restart or otherwise signal the active trainer.
WorkingDirectory=/home/inzi/poke-bot-agent-deployments/pure-rl-resident-v2
