[Service]
# Installed into the active user unit only after the exact passed checkpoint is
# frozen and both explicitly approved Kaggle submissions have been attempted.
Environment=PURE_RL_CONTINUE_AFTER_GATE=1
