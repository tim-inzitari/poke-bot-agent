# Generalized RL libs extraction

Additive library work lives in [`rl-libs/`](../rl-libs/) (v0.2.0 — all planned
extracts). It does **not** rewire production trainers, selectors, or live pins.

See [`rl-libs/README.md`](../rl-libs/README.md) and
[`rl-libs/docs/EXTRACTION.md`](../rl-libs/docs/EXTRACTION.md).

Standalone: branch `lib/rl-libs`, tag `v0.2.0`.

Sibling LAN dispatcher: `wave-dispatch` (`lib/wave-dispatch`, tag `v0.3.1`).
