# Dragapult (Blackwell, 512d) unattended overrides. Sourced after configs/unattended.env.
# Bound tensor-build RAM on the filtered ladder corpus: the full ~27GB merge previously
# peaked ~97GB. run_archetype.sh honors an existing TENSOR_BUILD_WORKERS (uses :- default).
TENSOR_BUILD_WORKERS=2
