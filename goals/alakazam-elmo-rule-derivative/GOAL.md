# Alakazam Elmo Rule-Derivative Goal Gateway

Schema: `poke_bot.goal_gateway/v1`

Revision: `32`

Root handoff revision: `334-DERIVATIVE-TERMINAL-REFRESH-DUAL-SUBMIT-STOP`

Status: `authoritative_for_stopped_iter14_terminal_expert_refresh_dual_submit_then_production_stop`

This is the stable goal entry point for the isolated Alakazam public-rule
representation and auxiliary-head experiment. It is a separate goal from the
production Pokemon RL controller. Revision 5 authorizes a future fail-closed
handoff from Elmo preparation to Inzi Blackwell training, one checksum-exact
Kaggle queue entry, and then derivative self-play across the verified available
fleet. It grants no immediate service/runtime change and no serving-selector or
production deployment authority. Revision 6 clarifies corpus row scope only;
it does not change the revision-5 lifecycle or authorize an immediate action.
Revision 7 changes only the composite warm-start parent. Revision 8 supersedes
the old 96-MiB publication/transfer-object limit with a binary 1-GiB limit and
accepts the already-running revision-7 re-featurization outputs without restart
or recomputation when their final immutable objects validate. Revision 9
supersedes revisions 2 and 8 for the active corpus pass: use the most recent 20
UTC days (2026-07-23 through 2026-08-11), process one day at a time with
intraday process lanes, and publish content-addressed rollout shards no larger
than 15,000,000,000 bytes.

Revision 10 is the immediate owner override and the only active task. Bootstrap
the current bug-fixed derivative policy structure from immutable r195 weights
mapped into the r274 head/architecture structure
checkpoint `sha256:261d367e131eeaacc62f86f8f0443250d187daf82bcbcaa88fafad7c9199cc3a`
using the already completed recent-20-day data and the established expert
packing/training process. Repack all twenty UTC-day archives from 2026-07-23
through 2026-08-11 against the current bug-fixed derivative policy and
representation; a pre-fix temporal tensor pack may not substitute for this
fresh twenty-day pack. Run 25 genuine full-corpus epochs. Every eligible
recorded training decision occurrence is visited once per epoch; semantic
pattern deduplication may not replace the optimizer dataset. Put every
architecture-present non-combo model weight in that r274 head structure in the optimizer, including the
backbone, policy, existing heads, Fusion, OwnDeck, matchup paths, and repaired
rule paths. The combo tensors remain only as dormant checkpoint/pack ABI bytes
because physical removal breaks the existing pack; combo loss is zero, combo
route is disabled, and combo tensors are excluded from the optimizer and
runtime. Preserve the prior 325-step/6,438-pattern sidecar-only run as invalid
historical evidence with no promotion, routing, or self-play authority. After
the full bootstrap seals and validates, build and submit exactly one
checksum-bound Kaggle package from that validated checkpoint, then immediately
start the existing derivative self-play loop. No submission may precede
bootstrap validation and this order authorizes no additional copies. Do not
alter the inactive r274 services.

Revision 11 makes Elmo's `/mnt/Main` ZFS pool the long-term archive for the
entire derivative/refeaturization lineage. Inventory and preserve finalized
Elmo artifacts there and call back create-only checksum-exact copies of every
sealed derivative artifact that currently exists only on Inzi, including
daily/final re-featurization shards, deterministic packs, checkpoints,
optimizer state, receipts, registries, packages, submission evidence, and
sealed self-play outputs. Inzi remains the active learner and keeps its local
working copies; return archival is replication, never a move or a training
gate. Mutable or open files are ineligible until their own durable boundary.
The superseded Elmo tmpfs spool may be removed from `/dev/shm` only after a
content-addressed ZFS archive, exact file/size/SHA-256 manifest, source/archive
parity, and durable receipt pass. Run archival at bounded low priority without
stopping, restarting, reconfiguring, or starving healthy training, collection,
Kaggle, inspector, storage, or interactive services. Refuse conflicts and
never tune or flush ARC/L2ARC merely to reclaim RAM.

Revision 12 defines the active derivative lifecycle after the validated
revision-10 bootstrap and its single initial Kaggle upload. Run exactly 20
self-play iterations, numbered 1 through 20. At each completed iteration-5
boundary (iterations 5, 10, 15, and 20), seal the self-play checkpoint and
collection first, then run exactly five full expert-refresh epochs over the
same checksum-bound recent-20-day pack used by the validated bootstrap, with
the same all-non-combo optimizer scope and combo-disabled boundary. Validate
and seal the refreshed checkpoint, then build and durably queue exactly one
checksum-bound Kaggle submission from that refreshed checkpoint. Its visible
Kaggle label/message must include at least the refreshed checkpoint's first 12
lowercase SHA-256 characters; the revision-10 initial submission omitted that
text even though its
queue and package receipts bind the checkpoint, and no duplicate corrective
upload is authorized. No submission occurs at iterations 1--4, 6--9, 11--14,
or 16--19. A quota or spacing delay may leave a milestone pending without
losing the checkpoint; no score is required to begin the next iteration once
the durable queue receipt exists. Do not collect iteration 21. The existing
derivative loop, receipt sequence, package identity gates,
full-available-fleet routing, r274 inactivity, and production-selector
prohibition remain unchanged.

Revision 13 changes only the post-bootstrap Kaggle cadence. Keep the five full
expert-refresh epochs at iterations 5, 10, 15, and 20, but build and durably
queue milestone submissions only after the refreshed iteration-10 and
iteration-20 checkpoints. Iteration-5 and iteration-15 refreshes continue
training immediately without a package or queue entry. The 12-character
lowercase checkpoint SHA-256 prefix remains mandatory in each iteration-10 and
iteration-20 visible label. No score wait, iteration-21 collection, r274
restart, RTP activation, combo activation, or production-selector authority is
added.

Revision 14 authorizes exactly one immediate, separately identified Kaggle
submission of validated derivative checkpoint
`sha256:8b59af9af1d715639bd3d63a84df7d608cee686c27aadf1c6dac3c971631a248`
using the exact `deck.csv` list from Kaggle submission `55468965`. Clone the
exact already-uploaded revision-10 package/runtime from submission `55486518`
and replace only `deck.csv`; every other archive member's content remains
byte-identical. Keep NO RTP, search/MCTS off, the same Matchup Adapter/runtime,
and `first_if_allowed`. The visible label states both `8b59af9af1d7` and
`55468965`. Build, smoke, checksum-bind, and upload once now with a fresh
single-use authorization. No retry, duplicate, second copy, or later reuse of
this override is authorized. Preserve both source packages, the checkpoint,
active derivative self-play, r274 inactivity, revision-13 milestone cadence,
and the production selector unchanged.

Revision 15 supersedes only revision 14's ordinary queue-spacing wait. Upload
the exact validated revision-14 package now. This authorizes exactly one
network upload attempt and no retry, duplicate, or second copy. Every package
identity, runtime, validation, preservation, and non-interruption requirement
from revision 14 remains unchanged.
The single actual upload was accepted as Kaggle submission `55487412` at
`2026-08-13T16:21:16.367Z` and completed with score `600.0`.

Revision 31 records the failure learned from derivative submission `55512502`
without interrupting the healthy iteration-5 collection. Kaggle episode
`93077626` ran the exact package against itself for 1,179 turns and 8,303
environment steps before seat 0 exhausted the 2,000-second run timeout. Both
seats legally recycled hand/deck/discard cards while Prize counts, damage/HP,
KOs, board energy, and the effective board position stopped progressing. This
is neither an illegal-action failure nor an inference-speed failure.

At the first clean boundary after iteration 5 seals, activate a training-only
no-progress termination and return override. After 64 complete turns with no
change to public win-relevant progress (Prize counts, HP/damage/KO/board
identity, attached energy, or special-condition state), terminate collection
with typed reason `no_progress_stall`. Hand, deck, and discard churn must not
reset the clock. Preserve the actual unfinished engine outcome separately, but
write terminal training return `-1.0` to both collected self-play seats so the
legal mutual-avoidance loop is not learned as a neutral draw. Preserve the
existing Prize-plan-v2 H3 formula and coefficient, replay membership/weights,
runtime/Kaggle action path, and genuine completed-game outcomes. This shaping
applies only to newly collected self-play after the boundary; do not relabel
sealed earlier shards and do not restart iteration 5.

Every future derivative upload must test the final tarball, not only its
checkpoint or an 80-step smoke. Run 64 independent exact-package mirror games
with two separately initialized package instances and require every game to
reach a genuine terminal result. Timeout, incomplete play, policy failure,
illegal action, the engine-step cap, or 768 no-progress turns is a hard
pre-network rejection. Every newly built explicit-profile package carries a
separate serving-only escape: after 512 complete turns without the same public
win progress, latch escape mode and bypass policy inference by selecting only
the legal `END` option at each subsequent MAIN prompt until libcg reaches its
real terminal result. Never invent END at a factorized/non-MAIN prompt, and do
not alter any action before turn 512. This package safeguard has no training
return semantics and does not enable search, RTP, or MCTS. The official libcg
binary draws shuffle entropy from the operating system and exposes no true seed
setter; record requested framework seed 0 but do not falsely claim native
seed-0 shuffle control. Bind the 64-game results, the 512-turn package escape,
and the 768-turn hard limit into the package attestation. The known failed
package `sha256:48bda09d…1302` receives no unchanged retry.

Revision 32 is the immediate terminal owner override. Stop before iteration 14
and do not resume or seal its incomplete collection. The managed derivative
trainer was stopped cleanly after the iteration-13 commit and before any
iteration-14 collection receipt existed. Preserve the incomplete
`shards/iter_00014.jsonl` bytes as ineligible recovery evidence; they may not
enter replay, optimizer, evaluation, packaging, or submission.

Starting from exact committed iteration-13 checkpoint
`sha256:60b3b4b697f203698de5a580fe65da376f1be4b97fc47c473d214cb7ec25331d`,
run exactly one quick, checksum-bound full expert-refresh epoch using the
existing recent-20 expert manifest, current bug-fixed policy/reward structure,
all architecture-present non-combo optimizer membership, and existing
streamed rehearsal implementation. Do not collect another self-play game and
do not run a formal holdout. Seal the refreshed child checkpoint and rehearsal
receipt before packaging.

From that one refreshed checkpoint, build and upload exactly two final
direct-policy packages. One uses the exact four-Enhanced-Hammer list with
ordered-card SHA-256
`e61c0a4ffcfeb730808ac561f39c1efa9de5f80aec577d3be82f3fc790b7dab2`;
the other uses the exact original r195 submission-55378392 deck file SHA-256
`1705f0f4db0c54b32f297fc9292a417b0c3abc9fdb6edf6a5370af6a635efe65`.
Both packages use the same refreshed checkpoint, current verified Matchup
Adapter/Fusion/direct-policy runtime, RTP off, MCTS/search absent, and the
revision-31 final-tarball 64-mirror gate. Labels must distinguish the deck and
include at least the first 12 lowercase characters of the refreshed checkpoint
SHA-256. Exactly one network upload per package is authorized; no third package
or iteration-14 restart is authorized.

After both accepted-or-quota/spacing-pending upload receipts are durable, stop
all managed derivative production collection/training workers through their
declared service managers. Preserve the shared Kaggle queue until both uploads
are accepted or durably pending, preserve dashboard/read-only monitoring, and
do not touch interactive sessions, r274 services, critic-owned work, or any
unrelated production lineage.

Revision 16 authorizes an isolated RTP shadow-training lane on Elmo using the
already sealed recent-20 complete-action overlay. It may bootstrap immediately
after a passing resource, identity, loader, forward/backward, and
noninterference preflight. The exact frozen derivative checkpoint
`sha256:8b59af9af1d715639bd3d63a84df7d608cee686c27aadf1c6dac3c971631a248`
is parent provenance and remains immutable. A checksum-bound existing RTP
sidecar may initialize planner weights, while a separately versioned public
semantic 40-to-96 adapter is learned jointly and stored in the new shadow
checkpoint. Training uses only the sealed overlay's train days; validation
uses only its validation days; evaluation days remain untouched. It runs under
a separate managed Elmo service with bounded resources and may never stop,
restart, reconfigure, or reduce the active Inzi derivative learner. The new
checkpoint is shadow-only, serving-ineligible, action-ineligible, and has no
selector, Kaggle, self-play, policy-gradient, or production authority. A later
explicit owner decision and separate promotion receipts are required before
runtime attachment.

Revision 17 adds one separately checkpointed, trainer-only reward/critic
sidecar without changing or delaying the already-running policy bootstrap.
Train it on Bert from the checksum-bound recent-20 recorded feature rollouts
with whole-episode and whole-day-disjoint splits. It has exactly eight outputs:
calibrated `V_win(s)` and action-conditioned `Q_win(s,a)` win probabilities,
plus state and chosen-action Prize-differential critics
`V_prize^h(s)` and `Q_prize^h(s,a)` for the next `h = 1, 2, 3` same-seat
decision frames. Preserve the existing model `value_head` and strategic
`action_q_head` unchanged. The new sidecar is excluded from the policy model
state dict, runtime, Kaggle packages, Fusion, selectors, and serving.

After the active bootstrap checkpoint is immutable and validated, but before
the next play-loop RL advantage materialization or optimizer unit begins, hot
swap only the trainer's advantage provider to the checksum-bound frozen
sidecar. For one complete recorded action, broadcast the same advantage across
all of its factorized selected stages:

`A = clip(0.75 * (z - V_existing(s))
          + 0.25 * (Q_win(s,a) - V_win(s))
          + 0.05 * (Q_prize^1(s,a) - V_prize^1(s))
          + 0.025 * (Q_prize^2(s,a) - V_prize^2(s))
          + 0.0125 * (Q_prize^3(s,a) - V_prize^3(s)), -A_max, A_max)`.

Here `z` is the observed terminal loss/draw/win return, not search. Each Prize
target is the clipped observed same-seat interval differential
`(own Prizes taken - opponent Prizes taken) / 3`; an incomplete or ambiguous
interval is masked, never assigned zero. Critic predictions are precomputed
and frozen for an actor unit, with no actor gradient into the sidecar. The
hot swap fails closed to the exact legacy advantage `z - V_existing(s)` on any
identity, alignment, split, coverage, calibration, finiteness, clipping, or
AWR effective-sample-size failure. It uses no GAE, TD(lambda), IQL, replay
weight changes, unchosen-action labels, simulator branching, rollout search,
RTP, or MCTS. This owner decision authorizes staging and boundary activation
of the trainer-only advantage provider; it does not authorize a bootstrap
restart, mid-epoch change, runtime action path, selector change, or submission.

Revision 18 returns Bert to the main derivative play-loop fleet after both the
active bootstrap checkpoint and the superseding revision-20 critic-canary activation receipt
are durable. Bert is a remote simulator/policy-leaf execution host only; Inzi
remains the sole learner and optimizer, and critic training or storage on Bert
does not grant optimizer or checkpoint-mutation authority. Admission requires
a fresh noninterference and capacity preflight, exact candidate checkpoint,
runtime, simulator, feature/action schema, critic-advantage configuration, and
opponent-package parity, plus healthy managed-worker evidence. Bind explicit
Bert worker/process, multi-environment packing, CPU/RAM/GPU, and network caps
in the routing receipt. Every returned child game retains its independent
global game, seed, seat, opponent, result, and trajectory identity. Existing
lease and duplicate suppression remain fail closed. A Bert fault excludes or
quarantines only its affected remote capacity and cannot stop the learner,
weaken exact collection counts, change the critic formula, or fall back to a
different checkpoint/device/package. Do not readmit Bert before the two
post-bootstrap receipts, restart the bootstrap, or modify an in-flight
optimizer or collection unit merely to add it.

Revision 19 returns Elmo to the main derivative play loop at the next immutable
collection commit after the sealed revision-16 bootstrap. Elmo's existing
compose-managed remote worker must be started and pass its health, exact
runtime/checkpoint/package parity, capacity, lease, and duplicate-suppression
gates before the single boundary restart that makes Inzi adopt the already
staged `192.168.1.143:8765` endpoint. Inzi remains the sole learner and policy
optimizer. Do not restart or modify the healthy in-flight collection merely
to add Elmo.

At that same boundary, launch a separate managed Elmo RTP shadow-continuation
unit warm-started from the exact revision-16 checkpoint
`sha256:aea301bbf958a671cc7ccfc07867b1c3d25b163ef1a2575488897bf28b587c20`.
The warm start must restore both planner weights and the stored 40-to-96 public
semantic adapter. It writes a new create-only output, consumes only the sealed
train and validation splits, leaves evaluation untouched, and may never enter
the action path, policy state dict, policy optimizer, Fusion, serving,
selectors, or Kaggle. Failure of the shadow lane may quarantine that lane but
must not corrupt or silently replace the main learner or remote-worker
identity; activation and completion require separate immutable receipts.

Revision 20 supersedes only revision 17's actor-advantage formula, activation
gates, and activation receipt. Keep its separate Bert-trained eight-output
sidecar, recorded chosen-action-only data, whole-episode and whole-day splits,
unchanged existing `value_head` and strategic `action_q_head`, and complete
runtime, package, Fusion, selector, search, RTP, MCTS, GAE, TD(lambda), IQL,
replay-weight, and unchosen-label exclusions.

Let `p_win(s)` be the frozen calibrated BCE state win probability and define
`Vwin_z(s) = 2 * clamp(p_win(s), 0, 1) - 1`. For the first enabled actor
canary, one complete chosen action receives

`A = (z - Vwin_z(s))
     + 0.05 * m1 * (Q_prize^1(s,a) - V_prize^1(s))`.

Broadcast that one scalar across every selected factorized stage. `m1` is one
only when the next same-seat recorded-action interval is complete, causal, and
unambiguous; otherwise mask only the one-frame Prize term, never fabricate a
zero target. `Q_win(s,a)`, `Q_prize^2 - V_prize^2`, and
`Q_prize^3 - V_prize^3` remain trained, frozen, logged diagnostic outputs with
actor coefficient exactly zero. They require a later explicit owner revision
and separate calibration canary before actor use. No actor gradient reaches
the sidecar, no additional raw-advantage clip is introduced, and the existing
AWR normalization and weight clipping remain unchanged.

The disabled and receipt-backed rollback mode executes the existing
`z - V_existing(s)` advantage branch exactly. Any identity, split, action or
stage alignment, target coverage, calibration, finiteness, heldout,
noninterference, or AWR effective-sample-size failure blocks activation or
returns to that explicit disabled mode at a safe boundary without rewriting a
policy checkpoint. This task alone owns the critic lane. Use exactly four
bounded, unique-object, SHA- and size-verifying streams for immutable Elmo to
Bert bootstrap transfer. Derive the compact Prize target overlay from the raw
public episodes on Elmo, seal it, and transfer it beside the complete-action
overlay and 40-wide feature pack; do not copy mutable inputs or disturb Elmo's
RTP shadow or the active trainer. Revision 18 now depends on this revision-20
activation receipt. Revision 19's Elmo remote/RTP boundary is otherwise
unchanged.

Revision 21 supersedes only revision 20's binary-win terminal baseline and its
activation receipt. Binary BCE `P(win)` treats both a draw and a loss as
non-win, so `2 * P(win) - 1` is not an estimate of the existing
loss/draw/win return. Keep the frozen existing state-value prediction for the
terminal term and use exactly

`A = (z - V_existing(s))
     + 0.05 * m1 * (Q_prize^1(s,a) - V_prize^1(s))`.

All eight sidecar outputs still train and receive separate calibration and
coverage receipts. `V_win`, `Q_win`, and Prize horizons 2 and 3 have actor
coefficient zero in this first canary. The disabled branch remains the exact
legacy `z - V_existing(s)` path. Revision 21 also pins the target-only overlay:
each interval begins at the pre-action public Prize counts for complete action
`i` and ends at the pre-action public counts for complete action `i+h` for the
same archive, episode, and acting seat. A missing later action, setup count,
terminal-ending interval, identity mismatch, or non-monotone Prize count masks
that horizon; it never becomes zero. The row and manifest retain explicit
before/after counts, action identities, per-horizon masks/reasons, and exact
raw-ZIP/complete-action-overlay digests, but no Prize-card identities or other
hidden information. Revision 18 now requires the revision-21 activation
receipt. Revision 19 remains unchanged.

Revision 22 adds only an inactive elastic-remote staging layer. Stage one
loopback-bound gateway that speaks the existing compressed whole-game remote
worker protocol to the trainer and routes behind that stable endpoint to one
or many manifest-listed simulator/GPU-leaf workers. The gateway registry is a
strict, complete snapshot. Each enabled backend declares a unique ID and
private/tunnel endpoint, an exact capacity, hostname, required job kinds and
capabilities, full active-checkpoint SHA-256, and its storage-local checkpoint
path. Before a new snapshot replaces the last known-good fleet, every new or
changed enabled backend must complete hello identity and storage-local digest
preflight. A failed candidate addition leaves the prior fleet intact; a
removal/change waits for admitted active jobs to drain. Reload and pin verify
the target object on every admitted backend before the first mutating control
command and remain subordinate to the trainer's existing strict
between-iteration digest hard gate.

This revision authorizes only repository source, an
`activation_allowed=false` registry, inert managed-service and AWS examples,
documentation, and tests. It does not start/install a gateway or worker,
provision or incur provider cost, create a tunnel/private route, expose a
worker port, change the active trainer endpoint, restart/preempt training,
change `known_candidate_hosts`, or admit/dispatch to a new host. The gateway
must bind loopback and use only authenticated tunnels or private routing to
workers. A later explicit owner activation revision must name the exact
gateway manifest/service identities, extend the candidate inventory if
needed, bind package/runtime/checkpoint and per-host capacity/routing receipts,
and schedule the one-time trainer endpoint adoption at a clean fresh
collection boundary. After that adoption, ordinary receipt-backed membership
changes may update the registry without editing trainer code/arguments or
mutating the optimizer/checkpoint. Revisions 18--21 otherwise remain
unchanged.

The staged operator path accepts one or many private/tunnel `IP[:port]`
values. It discovers and verifies each worker's advertised hostname, current
storage-local checkpoint path and full SHA-256, worker capacity, job kinds,
capabilities, and health before one locked, fsynced, atomic registry replace.
Any failed member rejects the whole batch. Updated workers advertise their
current checkpoint path; older workers require that one path as an explicit
operator argument. Listing, strict checking, dry-run addition, replacement,
and drained removal remain subordinate conveniences and add no activation
authority.

Revision 23 supersedes only the planned first H1 Prize actor term and its
activation receipt for this critic lane. It does not activate the critic,
change the current trainer, or alter unrelated revision-22 elastic-fleet
staging. Preserve the exact observed terminal term `z - V_existing(s)`, the
current policy/value/action-Q heads, the existing fixed eight-output sidecar,
and all no-search/no-RTP/no-MCTS/data-isolation boundaries. Do not add H1,
H3, or H6 contributions together: exactly one selected cumulative Prize-plan
horizon may ever have a nonzero actor coefficient so the same Prize event is
not repeatedly credited.

Stage a separately versioned Prize-plan-v2 sidecar unless a measured ABI
reconciliation proves an extension is safe; never silently repurpose the
fixed eight-output ABI. The new sidecar trains and receipts public
action-conditioned and state Prize-plan values for H1, H3, H6, and H12 from
recorded chosen actions only. A horizon consists of the next complete actions
by the same acting seat, with all intervening opponent activity included. H1
is retained as a trained/evaluation comparison arm with actor coefficient
zero under H3 mode; H6 and H12 are diagnostic with coefficient zero. The
first potential actor canary is H3 only and, after every receipt gate passes,
uses exactly:

`A = (z - V_existing(s)) + 0.025 * m3 * c3 * A_plan_3`

where `A_plan_3 = Q_plan_3(s,a) - V_plan_3(s)` is a frozen,
train-split-scaled action advantage. `m3` is one only for an exact causal,
complete three-segment same-seat interval. `c3` is a frozen, receipt-bound
confidence/support scalar from validated public inputs only; it may not depend
on hidden information, a live/unvalidated runtime signal, or an evaluation
split fit. The disabled, failed, or rolled-back branch executes the exact
legacy `z - V_existing(s)` code path, without a mixed fallback.

Prize-plan targets use only public Prize counts and exact public transition or
event evidence. Fit a smoothed monotone train-split-only Prize-race potential
`Phi(our_remaining, opponent_remaining) = 2 * P_iso(win | counts) - 1` and
checksum-bind the fitting inputs, fit configuration, and frozen table. The
potential must improve when our remaining Prizes fall and worsen when the
opponent's remaining Prizes fall. For each exact complete same-seat segment,
use `rP_t = gamma * Phi(s_{t+1}) - Phi(s_t)`; H3 is the discounted sum over
three such complete segments. `gamma` and all train-split scaling/support
parameters must be fixed and receipt-bound before fitting or actor use. Never
infer a terminal after-state, hidden Prize identity, or missing transition.
Missing, ambiguous, nonmonotone, or terminal-censored evidence is masked, not
assigned zero; `z` remains only in the legacy terminal term.

Training may consume only sealed, checksum-bound expert and later self-play
trajectories with whole-day, whole-episode, and group-disjoint splits. It is
local Bert critic training; Elmo may only perform read-only label extraction.
No unchosen-action label, simulator branching, policy state-dict attachment,
runtime critic call, GAE, TD(lambda), IQL, replay reweighting, RTP, MCTS, or
search is authorized. Required evaluation arms are exact legacy, current H1,
H3, and a later H6 candidate. Where capacity permits, each eligible arm runs
8,192 seed-matched seat-swapped games and reports win/draw/loss, Prize
conversion, matchup/Prize-count strata, late closeout, deckout, action entropy,
sidecar calibration/coverage, effective sample size, clipping, and action
disagreement. H3 promotion requires a positive paired win-rate result versus
H1 with a receipt-bound confidence interval and no material draw, rule-error,
ESS, clip, or action-entropy regression. H6 requires a later explicit canary;
H12 remains diagnostic unless explicitly promoted later.

Implementation and staging begin now, but the frozen trainer-only advantage
provider may hot-swap only at the first clean receipt-backed actor/optimizer
unit boundary after target-overlay, sidecar, calibration, action-support,
ESS/clip, noninterference, and rollback gates pass. Do not mutate an in-flight
collection or optimizer unit, restart or preempt healthy bootstrap/training,
or activate on a missing or failed receipt. Before materialization or
activation, reconcile this direction with the fresh reward audit and measured
stored transition schema. If the required potential/after-state target cannot
be produced exactly, record the measured blocker, keep legacy active, and
stage the closest valid diagnostic target rather than fabricating labels.
Revision 18's future Bert remote reentry consequently requires the
revision-23 H3 activation receipt rather than the superseded revision-21
receipt; revision 22 remains otherwise unchanged.

Revision 24 extends only revision 22's inactive elastic-fleet staging with the
selected AWS operator path. The initial shape is exactly two On-Demand
`g6.16xlarge` instances, each configured for 48 simulator workers and one
co-located L4 policy leaf; the same stack may later expand to exactly four.
The one-file operator accepts an AWS CLI profile, region, `$500` planning
ceiling, and either managed `auto` networking or an existing VPC/subnet.
Credentials remain in the normal AWS credential chain and never enter the
config, worker, checkpoint, registry, or receipt.

The managed network is an isolated private VPC/subnet with SSM and
SSM-Messages interface endpoints. Instances receive no public IP or inbound
security-group rule. Persistent Session Manager forwards carry the existing
compressed whole-game protocol; policy leaves stay co-located. The exact
Docker image streams directly from Elmo and checkpoints stream directly from
Inzi through authenticated SSM-backed SSH. S3, ECR, Tailscale, public SSH, and
public worker ports are not part of the data path. Checkpoints are
content-addressed and locally SHA-256 verified before admission or reload.

A two-to-four expansion initializes only the two new workers, preserves jobs
on the first two, hot-adds one atomic gateway snapshot, and inherits the
original absolute shutdown time. The conservative cost guard is `$4` per
node-hour plus `$20`: two nodes for 48 hours plan at `$404`; four for all 48
hours fail the `$500` ceiling. Late expansion counts the original horizon and
only new nodes' remaining whole hours. AWS Budgets is secondary because its
billing view can lag; pre-launch refusal and the shared instance-side deadline
are mandatory. The billable command also fails unless the separately
authorized one-time clean-boundary gateway/trainer adoption already exists.
Revision 24 still authorizes source, templates, documentation, and tests only:
it does not install tools, activate the gateway, change the trainer, provision
or bill AWS, create a live tunnel, restart training, or admit a host.

Revision 25 changes only future derivative evaluation volume. The already
running iteration-0 formal holdout remains sealed under its launch-time
4,500-game contract and must finish without interruption, resizing, replay, or
semantic mixing. Beginning with the next formal holdout dispatched after that
iteration-0 commit, evaluate exactly 50 games against each opponent deck in
the complete checksum-bound active formal-holdout roster: 25 with the
candidate in each seat. The total is derived from the roster size (currently
18 opponents, therefore 900 games) rather than fixed independently. Do not run
a separate research-control or research-game block in those future
iterations. Preserve the opponent roster, weights, thresholds, exact-game
audits, promotion semantics, and evaluation-training isolation. Apply this
change only at the next clean post-commit/pre-dispatch receipt-backed boundary;
if the migration is not ready there, retain the prior complete contract rather
than mixing evaluation semantics inside a dispatch.

Revision 26 is the owner's explicit activation decision for the already-trained
Prize-plan-v2 reward system. Finish the active iteration-3 optimizer, formal
holdout, and immutable commit with its existing legacy advantage semantics.
At the next clean actor/optimizer boundary, before iteration-4 collection or
optimizer work begins, activate the frozen H3 trainer-only additive reward:
`(z - V_existing(s)) + 0.025*m3*c3*(Q_plan_3(s,a)-V_plan_3(s))`. The exact
Prize-plan-v2 checkpoint, scale, support table, validation evidence, current
learner parent, and exact replay-window cache must be full-SHA-bound. Generate
that cache only from the sealed replay window's public recorded chosen actions;
mask unavailable or terminal-censored H3 intervals and broadcast each complete
action's one additive value across its factorized stages. This owner decision
accepts the existing heldout calibration plus exact-policy ESS/clip
nonregression as sufficient for the first bounded `0.025` canary and supersedes
revision 23's unrun 8,192-game-per-arm comparison as a pre-activation blocker;
that paired comparison remains required post-activation evaluation evidence.
Do not mutate iteration 3, retrain the sidecar, attach it to runtime/Kaggle, add
search/RTP/MCTS, change replay weights, or alter existing value/action-Q heads.
Any identity, coverage, finiteness, or cache failure restores the exact legacy
branch at a clean boundary.

Revision 27 is the owner's immediate, iteration-specific evaluation waiver.
Preserve the completed iteration-3 optimizer checkpoint and its completed
promotion comparison, but skip the already-dispatched 900-game formal holdout
for iteration 3. Record an immutable waiver bound to the exact run, iteration,
candidate checkpoint SHA-256, and this contract; make no measured holdout-pass
claim. Recover the trained candidate without retraining, commit iteration 3,
then perform the revision-26 clean-boundary migration so iteration 4 uses the
exact H3 reward formula. This waiver does not alter the formal roster,
thresholds, promotion comparison, or the revision-25 50-games-per-deck rule
for any later iteration. Research-control games remain disabled.

Revision 28 records the owner's explicit Blackwell optimizer-throughput
requirement. Do not interrupt or mutate the healthy iteration-4 optimizer or
race its already-armed revision-27 boundary adoption. At the first later clean
receipt-backed optimizer boundary whose implementation has fully validated,
replace repeated main-thread sparse-vector packing and avoidable host-to-device
pipeline bubbles with a bounded host-packed, pinned-memory, asynchronously
prefetched batch feed. Preserve exact replay membership and order, batch
boundaries, selected-action identities, factorized-stage semantics, losses,
AWR normalization and clipping, optimizer steps, gradients, and every model
and runtime head. Do not raise the 2,048-decision cap, change epoch count, add
gradient accumulation, use data-parallel training, or change reward, replay,
adapter, Fusion, guide, collection, or evaluation semantics under this
revision.

Activation requires an exact-checkpoint and exact-replay-window Blackwell
benchmark proving finite execution, loss/gradient/update parity under the
existing numerical contract, identical selected predictions, bounded host and
GPU memory, zero OOM fallback, exact disabled-path behavior, and a material
end-to-end optimizer wall-time improvement. A candidate that merely increases
instantaneous GPU utilization without reducing optimizer wall time remains
inactive. If validation misses a boundary, keep the current trainer unchanged
and stage it for the next later sealed boundary.

Revision 29 adds a separate simulator-throughput optimization lane that may be
implemented and benchmarked in parallel with the healthy learner, but may not
mutate or restart an in-flight collection, optimizer, evaluation, or boundary
adopter. Preserve exactly four environments per simulator process unless an
exact same-host benchmark proves a higher value increases completed-game GPS
without increasing failures, changing actions, results, seeds, deck order,
opponents, or resource bounds. Optimize the existing path through persistent
worker reuse, bounded cross-environment policy-leaf batching, immutable
card/deck encoding caches, and large buffered sequential result publication.
Do not change simulator rules, policy logits, legal-option order, RNG streams,
game allocation, replay eligibility, reward, search/RTP/MCTS state, or trainer
semantics.

Activation requires an exact checkpoint/package and fixed game-plan benchmark
against the current four-environment route, with identical terminal results,
selected actions and trace identities, zero missing/duplicate games, bounded
CPU/RAM/GPU use, and a material end-to-end GPS improvement. Elmo's idle GPU may
run isolated non-authorizing microbenchmarks when a fresh noninterference check
passes; the active Inzi learner and Elmo RTP/service workloads remain
untouched. A candidate that moves bottlenecks or raises utilization without
improving completed-game GPS remains disabled.

Revision 30 records the owner's clean-boundary activation and submission
cadence decisions after immutable iteration 4 committed. The bounded writer
buffer and immutable deck-encoding cache are active for the first live GPS
trial beginning with iteration 5; they change no simulator, policy, reward,
replay, search, RTP, MCTS, evaluation, or optimizer semantics. Keep Elmo's
current 36-worker/four-leaf configuration unchanged. Receipt-visible flush,
resource, completeness, and completed-game GPS telemetry determine whether the
trial remains active or rolls back at a later clean boundary.

Supersede only revision 13's submission iterations. Queue one milestone after
each fifth completed zero-indexed play-loop iteration: 4, 9, 14, and 19. The
iteration-4 package is the already sealed checkpoint
`sha256:0da1e00216779d061dfdfaf91b6b72d42c0515ce0ecf4edd6bfcf38e0cdb34b2`
and package
`sha256:48bda09d8e4d0f71b441ec55b0413d2b16b4c86fe1100550fd464cc275701302`.
Every such submission is direct-policy only: `use_mcts=false`, no packaged
search assets, `rtp_mode=off`, and no RTP sidecar. Use the exact current
Alakazam list with ordered-card SHA-256
`e61c0a4ffcfeb730808ac561f39c1efa9de5f80aec577d3be82f3fc790b7dab2`,
canonical multiset SHA-256
`a42e047c45c419a599a31f2e20a6209d324558082f27e12091ade8918376d182`,
and exactly four Enhanced Hammer (`card_id=1081`). A local validator rejection
before any network attempt may receive one exact-package retry authorization;
it is not a duplicate upload. Preserve the healthy learner, r274 inactivity,
the full checkpoint tensor registry, and production separation.

The sole typed canonical source for this experiment is
`goals/alakazam-elmo-rule-derivative/contract.json`. Implementation configs,
reports, manifests, and receipts are subordinate artifacts and must bind that
contract; none may become a second authority.

## Objective

Build and validate from the recent 20-day Elmo corpus an Alakazam derivative that repairs confirmed
public-rule representation collisions and honestly covers the eight
turn-checklist questions. After the frozen schema, revision-9 recent 20-day corpus,
collision census, and quarantined shard parity are complete, preserve healthy
r274 work to one clean durable boundary, then use exact receipts to hand the
new bootstrap/training lineage to Inzi's Blackwell. After successful bootstrap
validation, build and durably queue exactly one checksum-exact
`first_if_allowed` Kaggle submission; after the queue receipt is accepted or
pending only on quota/spacing, begin derivative self-play across the
checksum-proven full available fleet without waiting for a Kaggle score.

The intended result is:

1. a measured collision and rules-coverage report over raw Elmo episodes;
2. the smallest versioned representation and head changes justified by that
   report;
3. a frozen-parent Inzi Blackwell candidate;
4. simulator-rule tests plus paired benchmarks against unchanged r241/r274
   and immutable r195;
5. one single-use checksum-exact Kaggle queue handoff; and
6. receipt-backed derivative self-play across the full available fleet.

## Source and provenance

The current owner source is
`/Users/tsinzitari/.codex/attachments/8100a736-c437-41f2-b559-97f78d2e2f78/pasted-text.txt`
with
`sha256:0f440fc71043b4352e6401a3187c9d582c1c5614d76e186095e0eef51017af6f`.

Root revision 298 and its attachment
`/Users/tsinzitari/.codex/attachments/7b1d4464-b2f6-4fcc-8a6e-52abc35e3aaf/pasted-text.txt`
(`sha256:d3f06071663dde2ae7012da72b407b410c7facd06d09ab723cad05af44ddb2cb`)
remain immutable historical provenance. Root revision 302 handed experiment
design to this gateway. Root revision 303 and
`state/alakazam-new-list-direct-policy-r241.json` now own only the exact
production training-lineage transition, service boundary, Kaggle queue, and
fleet activation; they do not duplicate representation or target semantics.
Revision 6 is an explicit owner clarification recorded on 2026-08-12 without
a new attachment; the two attachment identities above remain provenance, not
the source of this narrower row-eligibility rule.
Revision 8 is the owner's explicit 2026-08-13 shard-size correction and has no
new attachment; it changes neither corpus rows nor model semantics.
Revision 9 is the owner's explicit 2026-08-13 execution correction and has no
new attachment. It narrows only the active corpus window, changes processing
to one independently receipted day at a time with intraday process lanes, and
raises the final rollout-shard ceiling to 15 GB decimal.

## Dedicated decision ledger

| Revision | Recorded at (UTC) | Decision |
|---|---|---|
| 1 | 2026-08-12 | Establish the separate, isolated Elmo-only goal, public-information contract, zero-gated frozen-backbone derivative, Phase A--C workflow, eight-channel trace contract, and three paired evaluation cohorts. |
| 2 | 2026-08-12 | Make the latest complete contiguous raw archive window, 2026-07-13 through 2026-08-11 UTC inclusive, mandatory for Phase A and re-featurization: exactly 30 distinct UTC partitions, one validated and deduplicated checksum-bound manifest, no silent 20-day/subset/adjacent-window fallback, no recollection without proved absence, and source/day/group-disjoint splits. Permit isolated jobs to use all Elmo CPU/GPU under a hard 96-GiB experiment-RAM ceiling, serialize memory-heavy phases, allow normal existing TrueNAS/ZFS L2ARC caching, prohibit ZFS/ARC/L2ARC tuning and managed-service changes, and require resource-peak receipts. |
| 3 | 2026-08-12 | Implement the requested isolated versioned representation, target, and checklist-provenance paths and freeze their feature schema first; then immediately validate and re-featurize the exact revision-2 30-day corpus. The collision census/report may be produced during or from that same pass rather than blocking zero-inert implementation. Corpus validation and census evidence still gate every nonzero activation, candidate-training step, and BO250. Unsupported branches remain zero/inert and may be rejected after census. |
| 4 | 2026-08-12 | After each immutable re-featurization/census shard is complete, at most 96 MiB, and individually schema/SHA/size validated, transfer it concurrently through bounded lanes into one quarantined create-only Inzi staging directory. Use full-content-addressed names, bind every object to the frozen schema and 30-day manifest, verify remote SHA and size, skip exact matches, refuse conflicts, and finish with a source/remote parity receipt. This is byte-staging authority only: Inzi may not load or use the artifacts. Preserve Card2Vec completely—learned embeddings, inputs, text-derived knowledge, checkpoint tensors, and behavior remain frozen/untouched. The new structured simulator residual is additive alongside/after Card2Vec, never a replacement; legacy text hashes retain only their existing frozen role and cannot provide exact mechanics authority to the new residual. |
| 5 | 2026-08-12 | After Elmo implementation/schema freeze, exact 30-day re-featurization/census, branch adjudication, and verified shard staging, supersede the current Inzi r274 training lineage at one clean receipt-backed boundary. Let current healthy r274 work seal first; fully pause only its trainer and r274 submission-boundary services, preserve the shared Kaggle queue service and all immutable rollback artifacts, make staged shards training-eligible atomically, and bootstrap/train the derivative on Inzi's `cuda:1` RTX PRO 5000 Blackwell from the exact sealed r274 parent with Card2Vec/backbone/existing heads/Fusion/OwnDeck/Matchup Adapters frozen. After validation, build and durably queue one checksum-exact `first_if_allowed` Kaggle package on the exact new list; then begin derivative self-play across the fully inventoried/checksum-matched available fleet without waiting for score. No runtime/service change occurs while recording this revision; no serving selector is authorized. |
| 6 | 2026-08-12 | Scan every episode in the exact 30-day raw audit, but materialize re-featurized feature/target/checklist rows and training/validation/evaluation rows only for a decision's acting seat when that same seat's recorded setup deck contains card ID 743. Literal membership is the complete predicate: an exact pilot-list match is not required and there is no tech-inclusion exception. Exclude decisions made by a non-qualifying opponent seat even when the other seat qualifies; include each seat independently when both qualify. Preserve the acting setup-deck canonical multiset digest on every included row and in list-variant strata. Accept unchanged revision-5 raw manifests, frozen schema bytes, and correctly scoped outputs through a checksum-bound revision-6 migration receipt; do not recompute solely because the goal/contract revision changed. No runtime/service action is authorized. |
| 7 | 2026-08-12 | Warm-start the derivative bootstrap from immutable r195 for every exact-name/shape-compatible legacy backbone and policy tensor because r195 is the strongest Alakazam weight parent. Fill architecture additions absent from r195—newer auxiliary heads, Fusion/OwnDeck additions, and matchup adapters—from the exact clean-boundary r274 checkpoint. Initialize only the genuinely new rule-residual/repaired-head parameters separately. Require a per-tensor source map, reject ambiguous overlaps or shape drift, keep every inherited tensor frozen during initial bootstrap, and preserve both checkpoints unchanged. r274 remains the same-architecture evaluation baseline and rollback source; r195 is now the primary compatible-weight warm-start source rather than only a benchmark. Accept unchanged revision-6 corpus/schema/census artifacts without recomputation. No immediate runtime/service action is authorized. |
| 8 | 2026-08-13 | Supersede revision 4's 96-MiB final shard and transfer-object limit with at most one binary GiB (1,073,741,824 bytes). Preserve deterministic routing, content addressing, schema/SHA/size validation, create-only publication, and the prohibition on transferring private partials. Let the active revision-7 Elmo/Inzi re-featurization runs finish without restart or recomputation; accept their immutable final objects through a checksum-bound revision-8 migration receipt. Inzi-local results remain on Inzi, while finalized Elmo objects may transfer create-only to the new revision-8 quarantine root. This changes no row scope, model, training, service, selector, or activation authority. |
| 9 | 2026-08-13 | Replace the oversized 30-day distributed pass with the most recent 20 contiguous UTC days, 2026-07-23 through 2026-08-11. Process each UTC day independently, using multiple intraday worker processes and one sequential private spool per lane; distinct hosts may process distinct days concurrently. The parent deterministically merges each completed day into immutable content-addressed rollout shards no larger than 15,000,000,000 bytes. Preserve superseded Elmo/Inzi partials as ineligible recovery evidence, never double-count them, and do not change r274 or any managed service. Existing validated daily ZIP receipts are trusted; do not repeat payload-level integrity/dedup hashing. |
| 10 | 2026-08-13 | Override the bootstrap with the fresh bug-fixed recent-20-day pack, immutable r195 weights mapped into the r274 head structure, 25 genuine full-corpus epochs, every architecture-present non-combo weight trainable, combo ABI tensors retained but loss/route/optimizer disabled, then exactly one validated Kaggle submission followed by derivative self-play. |
| 11 | 2026-08-13 | Make Elmo `/mnt/Main` ZFS the durable long-term archive for the complete derivative/refeaturization lineage, including create-only checksum-exact return copies of sealed Inzi-only artifacts. Preserve active Inzi working copies, copy only immutable boundaries, and archive/verify the superseded Elmo tmpfs spool before reclaiming its RAM. Archival is low-priority replication and grants no training, selector, serving, submission, or service-preemption authority. |
| 12 | 2026-08-13 | Run exactly 20 derivative self-play iterations. At completed iterations 5, 10, 15, and 20, seal the iteration, run exactly five full expert-refresh epochs on the same bound recent-20-day pack with all non-combo weights trainable and combo disabled, validate the refreshed checkpoint, and queue exactly one checksum-bound Kaggle submission whose visible message contains at least the first 12 lowercase refreshed-checkpoint SHA-256 characters. Do not collect iteration 21. |
| 13 | 2026-08-13 | Keep expert refreshes at iterations 5, 10, 15, and 20, but submit derivative milestones only at refreshed iterations 10 and 20. |
| 14 | 2026-08-13 | Immediately submit exactly one package using derivative checkpoint `8b59af9af1d7…` and the exact deck list from Kaggle submission `55468965`. Clone submission `55486518`, change only `deck.csv`, visibly label both identities, retain NO RTP/search-off, and leave active self-play and revision-13 cadence unchanged. No retry or second copy. |
| 15 | 2026-08-13 | Upload the revision-14 package now without waiting for the ordinary queue spacing boundary. Completed as Kaggle submission `55487412`, status `COMPLETE`, score `600.0`: one actual network upload, zero retries, and no second copy. |
| 16 | 2026-08-13 | Prepare and bootstrap a separately managed Elmo RTP shadow sidecar from the sealed recent-20 base-pack-plus-overlay view. Bind the frozen derivative parent and optional RTP warm start by full SHA-256, learn only a versioned 40-to-96 semantic adapter plus RTP planner, preserve whole-day splits, and keep the result shadow-only with no policy, selector, serving, Kaggle, self-play, or active-learner authority. |
| 17 | 2026-08-13 | Train on Bert one separately checkpointed eight-output action critic over the recent-20 feature rollouts, then hot swap its frozen composite advantage into the play-loop trainer only after the active bootstrap checkpoint seals and before the next RL optimizer unit. Preserve the existing `value_head` and strategic `action_q_head`; keep runtime, Kaggle, RTP, MCTS, search, replay weights, and model state dict unchanged. |
| 18 | 2026-08-13 | After the bootstrap and revision-17 critic activation receipts are durable, readmit Bert to the main derivative play loop as a checksum-matched remote simulator/policy-leaf host. Inzi remains the sole learner/optimizer; Bert receives no checkpoint mutation or selector authority. |
| 19 | 2026-08-13 | At the next immutable collection boundary after the sealed RTP bootstrap, start and health-check Elmo's compose-managed remote worker, restart the derivative loop once to adopt its already-staged endpoint, and concurrently launch a separate shadow-only RTP continuation warm-started from the exact bootstrapped planner plus semantic adapter. Inzi remains sole learner; no mid-collection change or RTP action/serving authority. |
| 20 | 2026-08-13 | Supersede only revision 17's first actor formula and activation receipt. Convert calibrated state win probability to `[-1,+1]`, add only the masked one-frame Prize action advantage at `0.05`, train but keep `Q_win` and Prize horizons 2/3 actor-inert, preserve exact legacy fallback, and bind the four-stream Elmo-to-Bert critic bootstrap to this task. Revision 18 Bert reentry now requires the revision-20 receipt; revision 19 is unchanged. |
| 21 | 2026-08-13 | Correct revision 20's binary-win/draw scale mismatch before training or activation. Keep the existing frozen value baseline in the terminal term, add only the masked one-frame action-conditioned Prize advantage at `0.05`, and pin the exact typed pre-action-to-pre-action same-seat target overlay and masks. All eight outputs still train; win and horizons 2/3 remain actor-inert. Revision 18 now requires the revision-21 receipt; revision 19 is unchanged. |
| 22 | 2026-08-13 | Stage, without activation, a loopback-only manifest-driven fleet gateway and templates so a later one-time clean-boundary trainer adoption can support one-or-many checksum-gated CPU-simulator/GPU-leaf remotes through atomic registry changes. Preserve the current candidate list and active trainer; no service, provider, network, host-admission, checkpoint, optimizer, selector, or training action is authorized. |
| 23 | 2026-08-13 | Supersede the planned H1 Prize actor canary with a staged, separately versioned public Prize-plan-v2 H3-only canary. Preserve exact `z - V_existing(s)`, fixed eight-output ABI, and every runtime/search/data-isolation boundary; train/receipt H1/H3/H6/H12 plan diagnostics from sealed chosen-action trajectories, but permit only `0.025*m3*c3*(Q_plan_3-V_plan_3)` after all receipt gates at a clean actor/optimizer boundary. Fit a SHA-bound train-only monotone public Prize-race potential, mask any unavailable/censored segment, evaluate exact legacy/H1/H3/later-H6 arms, and fail closed to exact legacy if schema reconciliation or any gate fails. Revision 22 remains unrelated and inactive. |
| 24 | 2026-08-13 | Extend only inactive revision-22 fleet staging with a one-file AWS operator for two `g6.16xlarge` nodes initially and four later. Use private SSM endpoints, no inbound/public worker port, direct Elmo-image and Inzi-checkpoint streams with no S3/ECR/Tailscale data path, content-addressed SHA verification, a conservative `$500` refusal guard, one shared 48-hour absolute expiry, and new-node-only hot expansion. Refuse billing until the separately authorized gateway/trainer adoption exists; do not install, activate, provision, tunnel, restart training, or admit a host under this revision. |
| 25 | 2026-08-13 | Leave the active iteration-0 4,500-game evaluation unchanged. For each future formal holdout, run exactly 50 games per checksum-bound opponent deck with a 25/25 candidate-seat split, derive the total from the complete active roster (currently 900 for 18 decks), and run no separate research-control/research-game block. Preserve roster, weights, thresholds, auditing, promotion semantics, and evaluation isolation; activate only at the next clean post-commit/pre-dispatch boundary. |
| 26 | 2026-08-14 | Activate the already-trained Prize-plan-v2 H3 reward system at the first clean boundary after iteration 3 commits. Preserve iteration 3 unchanged; bind the exact sidecar and per-replay cache, use only the bounded `0.025` H3 additive term over legacy `z-V_existing`, and fail back to exact legacy on any mismatch. The earlier 8,192-game-per-arm comparison becomes post-activation evidence rather than a blocker for this first bounded canary. |
| 27 | 2026-08-14 | Waive only iteration 3's formal holdout, preserve its trained candidate and promotion comparison, commit without a measured holdout-pass claim, and activate the already-authorized H3 reward before iteration 4. |
| 28 | 2026-08-14 | Stage receipt-gated Blackwell optimizer-feed packing and prefetch without changing the active optimizer or any training semantics. |
| 29 | 2026-08-14 | Stage simulator throughput improvements with exact behavior parity and a completed-game GPS gate. |
| 30 | 2026-08-14 | At the sealed iteration-4 boundary, activate the 16-MiB sequential shard buffer and immutable deck-encoding cache for iteration 5; leave Elmo unchanged. Submit the sealed zero-indexed iterations 4, 9, 14, and 19 using the exact four-Enhanced-Hammer Alakazam list and direct-policy package with MCTS/search and RTP absent. Permit one exact-package retry only for a proven pre-network local-validator rejection. |
| 31 | 2026-08-14 | Preserve live iteration 5, then at its sealed boundary terminate new self-play after 64 complete turns without public win progress and assign training-only return -1 to both collected seats while retaining the actual unfinished engine outcome. Require 64 independently shuffled exact-final-package mirror games to terminate before every future upload; hard-fail timeout, incomplete, illegal/fail-closed, step-cap, or no-progress cases. Official libcg shuffle seeding is opaque, so record framework seed 0 without claiming native seed control. |

## Revision 9 active corpus and execution boundary

Revision 9 is the active corpus authority and supersedes every older statement
in this file that forbids a 20-day subset or requires the active pass to cover
30 days. Use exactly the recent contiguous window 2026-07-23 through
2026-08-11 UTC inclusive. The existing 30-day raw manifest and freeze/parity
receipts remain immutable predecessor evidence; derive the recent-20 selection
without repeating ZIP payload validation or deduplication.

Only decisions made by an acting seat whose recorded setup deck contains
literal card ID 743 are materialized. Exact pilot-list identity is not
required and there is no tech exception. Each host may work on a distinct UTC
day concurrently, but each host processes only one day at a time and uses
intraday process lanes. Workers write one sequential private spool apiece;
the parent performs the deterministic merge and final publication. Every
final rollout shard is content-addressed, individually receipted, and at most
15,000,000,000 bytes. Private partials are never transferred or training-
eligible. This revision grants no r274, managed-service, training-activation,
or serving-selector action.

## Revision 11 Elmo ZFS long-term archive

Elmo's ZFS archive is the durable preservation destination for all completed
artifacts in this goal, regardless of whether Elmo or Inzi produced them.
Publish beneath the typed contract's fixed archive root with content-addressed
objects and immutable manifests. Copy from Inzi rather than removing Inzi's
local working bytes. A source object is eligible only when it is closed,
checksum-bound, and covered by its own completion or sealed-boundary evidence;
an active checkpoint, open log, temporary file, partial shard, or mutable
registry is not eligible yet.

Every archival batch records the exact source host/path, source identity,
source size and SHA-256, destination path, destination size and SHA-256,
copy/skip disposition, and conflict state. Publication is create-only and
atomic; an existing exact object is verified and skipped, while any conflict
fails closed. The final inventory must cover Elmo-local and returned Inzi
artifacts and identify anything still pending because it is active or unsealed.

The stopped superseded Elmo spool at
`/dev/shm/alakazam-refeature-2332383-1` remains ineligible recovery evidence,
but preservation no longer requires it to occupy tmpfs indefinitely. First
archive its complete tree to ZFS and verify an exact file-count, byte-count,
relative-path, file-type, SHA-256, and size manifest with no open handles or
links escaping the source root. Only after the durable archive receipt passes
may the exact tmpfs source tree be removed to reclaim RAM. Do not delete any
Inzi source after return replication, and do not flush or retune ZFS caches.

## Authority and ground truth

- Phase A scans every episode in the latest complete contiguous 30-day raw
  expert archive on Elmo, exactly 2026-07-13 through 2026-08-11 UTC inclusive:
  exactly 30 distinct UTC day partitions bound by one validated, deduplicated
  manifest SHA-256. No deck, seat, archetype, or row-eligibility prefilter may
  narrow this raw audit. A 20-day, adjacent, or other subset/window is not a
  fallback. Recollection is forbidden unless the manifest/audit first proves
  the needed source evidence absent.
- The exact pinned official libcg binary, identified by SHA-256, is gameplay
  ground truth. Its emitted legal option list is authoritative.
- Official paper rules or prose are secondary when they differ from libcg.
- r195 is the best operational reference, but it has an older architecture and
  a somewhat different native deck list.
- Unchanged r241/r274 is the fair same-architecture baseline for new heads and
  matchup adapters.
- “Next Alakazam line” means a replacement attacker line on the Bench. The
  current Active Alakazam never satisfies that channel.

## Hard boundaries

- Until revision 6's complete readiness, corpus-scope migration, and activation
  receipts exist,
  revision 8's exact one-GiB quarantined create-only shard transfer remains the only
  permitted Inzi action. Staged shards may not be loaded or used early. This
  metadata update itself performs no Inzi file, runtime, worker, service,
  checkpoint, queue, submission, or collection action.
- Preserve current healthy r274 training until a clean durable boundary. Never
  pause mid-collection, mid-shard, mid-optimizer, mid-adapter, mid-checkpoint,
  or mid-commit. At handoff, fully pause only
  `pokebot-alakazam-r274-rl.service` and
  `pokebot-alakazam-r274-rl-submission-boundaries.service` through user systemd;
  never signal processes directly. The shared Kaggle queue service remains
  untouched.
- Preserve parent checkpoints and current feature artifacts immutably.
- The separately versioned derivative is zero-gated. With the new path off, it
  bypasses completely and reproduces baseline logits bit-for-bit.
- Preserve the complete existing Card2Vec system: learned embeddings, current
  inputs, text-derived knowledge, checkpoint tensors, and runtime behavior are
  frozen and untouched. The structured simulator residual is additive
  alongside/after Card2Vec, never a replacement or reinterpretation. Existing
  text hashes may remain inside the frozen legacy/Card2Vec path, but they are
  not exact mechanics authority and cannot generate mechanics fields or labels
  for the new residual.
- Policy, value, Fusion, checklist logic, adapters, and search use only the
  acting player's public information set.
- Exact opponent-hand identities, opponent deck order, and unrevealed Prize
  identities are never runtime inputs. Privileged fields are separately typed
  target-only labels and cannot choose teacher/search/policy actions.
- Do not infer unsupported mechanics from text hashes. Missing or malformed
  evidence is reported as unavailable rather than guessed.
- Isolated jobs may use all available Elmo CPU and GPU capacity, but aggregate
  experiment RAM has a hard ceiling of 96 GiB. Only one memory-heavy phase may
  run at a time.
- Normal existing TrueNAS/ZFS L2ARC caching is allowed. Do not tune, resize,
  flush, replace, or otherwise reconfigure ZFS, ARC, or L2ARC, and do not
  create, edit, stop, restart, reload, or replace managed services for this
  experiment.
- Every isolated-job receipt reports measured resource peaks, including its
  RAM accounting method and peak, CPU/process peak, and per-device GPU memory
  and utilization peaks where applicable.

## Revision 3 execution order

1. Implement the requested isolated, separately versioned representation,
   target, and checklist-provenance changes. Every new feature branch is
   default-zero and inert; this step grants no training or action authority.
2. Freeze the complete feature/target/provenance schema and issue a
   checksum-bound schema manifest and zero-bypass receipt.
3. Immediately validate, deduplicate, and re-featurize the exact 2026-07-13
   through 2026-08-11 UTC corpus under that frozen schema. Generate the full
   collision census during this pass or deterministically from its immutable
   outputs; a separate pre-implementation census is not required.
4. Use the completed corpus-validation and census receipts to adjudicate each
   branch. A branch without support stays exact-zero/inert and may be rejected;
   it cannot silently become trainable or active.

No derivative branch may receive nonzero activation and no candidate training
or BO250 may begin until the exact 30-day corpus and full census validate.

## Revision 4 create-only transfer staging (historical size superseded by revision 8)

As soon as one re-featurization/census shard is complete and immutable, is no
larger than one binary GiB (1,073,741,824 bytes), and passes its individual schema,
SHA-256, and exact-size validation, it may transfer in parallel with the
remaining Elmo pass. The destination is the quarantined, non-runtime,
create-only Inzi directory
`/home/inzi/poke-bot-agent/outputs/quarantine/alakazam-elmo-rule-derivative/g8-one-gib-refeaturized-census-shards`.

Revision 8 supersedes only the historical 96-MiB size threshold below. Each
object uses its complete lowercase SHA-256 in its filename, binds the
frozen feature/target/checklist-provenance schema digests and validated 30-day
raw-manifest SHA-256, and is published immutably. Transfer uses at most four
parallel lanes; every individual object is at most one binary GiB. A remote object is complete
only after post-transfer SHA-256 and exact-size verification.

The copy path is resumable and idempotent: a byte-exact verified destination is
skipped, an incomplete object may resume through a private partial name before
atomic create-only publication, and any existing conflicting final name or
content is refused without overwrite, delete, rename, or replacement. After
all source shards complete, a final receipt proves source/remote manifest,
object-count, filename, SHA-256, and size parity.

The revision-8 destination is
`/home/inzi/poke-bot-agent/outputs/quarantine/alakazam-elmo-rule-derivative/g8-one-gib-refeaturized-census-shards`.
Private `.partial` files and worker spools are never transferable. Inzi-local
finalized objects remain on Inzi and enter the same checksum inventory without
an unnecessary round trip. This grants only directory creation and create-only transfer/staging authority.
The Inzi artifacts remain quarantined and training-ineligible. No Inzi load,
decode, import, mmap, cache warm, training, collection, selector, runtime,
worker, service, checkpoint, package, submission, propagation, or activation
is authorized. No managed-service creation or change is authorized.

## Revision 5 receipt-backed Inzi handoff

Revision 4 staging receipts alone never authorize use. After the frozen schema,
exact 30-partition corpus, complete census/branch adjudication, source/remote
parity, exact r274 parent, frozen-tensor identity, Blackwell preflight, and
rollback plan all pass, seal one readiness receipt. Let every current r274
collection/shard, optimizer/adapter step, checkpoint, and commit finish first.
At the next idle boundary, pause exactly the r274 trainer and r274
submission-boundary units through user systemd, seal their checkpoint and
optimizer state, leave the shared Kaggle queue service unchanged, and issue the
handoff activation receipt. Only that receipt atomically makes the staged
shards training-eligible and authorizes the new managed derivative bootstrap.

The bootstrap is a checksum-bound composite warm start. Immutable r195 supplies
every exact-name/shape-compatible legacy backbone and policy tensor. The last
immutable safety-approved r274 checkpoint sealed at the boundary supplies only
architecture additions absent from r195, including newer heads and matchup
adapters. A per-tensor source map must cover every inherited tensor exactly
once and reject ambiguous overlaps or shape drift. “Overwrite” means the derivative becomes the sole active Alakazam
training/collection lineage. It never means modifying or deleting r274 bytes.
All r274 checkpoints, optimizer state, registries, service definitions, and
receipts remain checksum-bound rollback evidence. Card2Vec, the neural
backbone, existing heads, Fusion, OwnDeck routes, and Matchup Adapters remain
bit-identical and absent from the new optimizer; only the new adapter, repaired
heads, and census-supported gates train on Inzi `cuda:1` (RTX PRO 5000
Blackwell). Any identity, resource, finiteness, frozen-tensor, layer-off, load,
or launch failure fails closed into the receipt-backed rollback plan.

After the Blackwell bootstrap candidate passes engine, information-set,
frozen-tensor, and package validation, build exactly one package using the
exact new-list deck and the bound runtime, simulator, catalog, schema,
checklist, router, and adapter identities. Durably enqueue its one single-use
`first_if_allowed` authorization in the existing shared Kaggle queue. Quota or
spacing may leave it pending; upload failure or delay never changes, deletes,
or falsifies the checkpoint, and no result is called submitted without an
upload receipt.

Once the durable queue receipt says accepted or pending only on quota/spacing,
self-play may start without waiting for upload or score. Inventory every known
candidate host, record availability/exclusion evidence, checksum the exact
package on every eligible host, and bind explicit per-device worker routing and
resource caps. Inzi Blackwell remains the sole learner/optimizer. Use globally
unique collection/game/lease identities, append-only shards, and exact
duplicate suppression so no game is counted twice. No old r274 training or
collection may run concurrently. Fleet inventory, package parity, routing,
activation, and per-shard receipts are mandatory; an unreceipted host drop,
device fallback, service change, or resource-cap weakening fails closed.

Exact typed receipt schemas and their closed top-level field inventories are
owned by `contract.json`. Receipts contain no self-referential digest field;
each receipt's exact bytes are SHA-256-bound by its consuming receipt.

## Phase A — raw-corpus audit

First validate and deduplicate one manifest covering the full existing Elmo raw
expert corpus from 2026-07-13 through 2026-08-11 UTC inclusive. It must
enumerate exactly those 30 distinct contiguous UTC partitions and carry its
SHA-256. Missing, duplicated, overlapping, non-UTC, or adjacent-window
partitions fail closed; no code may silently substitute the older 20-day
window or another subset/window. A recollection request is ineligible until
this audit proves the required evidence absent. The audit enumerates and scans
every raw episode before any deck or acting-seat predicate is applied.

The zero-inert implementation and frozen schema now precede this full audit.
Inventory the raw Elmo schema across all 30 validated partitions during the
same re-featurization pass or deterministically from its immutable outputs.
Classify every candidate field as a direct public libcg field, immutable public
catalog metadata, transition-derived target, fresh-simulator target, or hidden
information. The completed audit, not pre-audit assumptions, determines which
frozen branches may become training-eligible.

For every actor-visible selection frame, census `SelectContext`,
`effect`/`contextCard`, the complete raw option, current feature signature,
selected option, and adjacent public successor when available. Measure all
NUMBER values, especially 4 versus 5; same-card SKILL options with distinct
serial/source; duplicate ATTACK payloads; and distinctions in count, source,
target, effect, and remaining budget. Report frequency, selected-action
involvement, matchup distribution, and deck distribution.

A collision is actionable only when publicly distinguishable states or options
have different simulator semantics. Hidden-state equivalence and harmless
permutation equivalence are not bugs.

## Revision 6 acting-seat row scope

The raw audit and its field/collision census scan every episode in all 30
validated partitions. Row materialization is narrower. For each actor-visible
decision, inspect only that decision's acting seat and that same seat's exact
recorded setup deck. Materialize the re-featurized feature, target, and
checklist-provenance row if and only if the setup deck contains at least one
card ID `743` (Alakazam).

This literal membership test is the complete eligibility predicate. Do not
require the exact pilot list, its digest, a deck/archetype label, a minimum
Alakazam-line count, or any other core-card signature. There is no
tech-inclusion exception: a deck containing `743` qualifies even if another
classifier would call that copy a tech. If only one seat qualifies, exclude
every decision made by the other, non-Alakazam seat. If both seats qualify,
include each seat's own acting decisions independently. If neither qualifies,
the episode still contributes to the all-episode raw audit/census but produces
no re-featurized rows. Missing or malformed setup-deck evidence is never
guessed; the affected seat's rows are excluded with an explicit reason and
count.

Every included row preserves `acting_deck_multiset_sha256`, computed from the
complete recorded acting-seat setup-deck card-ID multiset using the existing
canonical sorted-array JSON encoding. Train, validation, and evaluation
manifests retain that digest and report per-digest row/game counts as list-
variant strata. The digest is provenance/stratification, not an exact-pilot
eligibility gate and not a new hidden opponent-deck runtime input.

Revision 6 does not change frozen feature, target, checklist-provenance,
catalog, simulator, Card2Vec, or zero-bypass schema bytes. The exact revision-5
30-day raw manifest remains valid when it already enumerates and scans every
episode. Revision-5 schema/freeze/parity receipts and correctly filtered
re-featurized shards remain immutable predecessor evidence and may be accepted
without recomputation after a revision-6 scope-migration receipt binds their
digests and proves this row predicate, seat exclusion, and per-list strata.
Incorrectly scoped outputs must be deterministically filtered or rematerialized,
but the unchanged raw audit and frozen schemas are not rebuilt merely for the
revision bump.

## Phase B — minimal versioned representation repair

Before the 30-day pass, implement the requested repairs supported by this
contract or the official API as default-zero, inert, separately versioned
branches and freeze their schema. Preserve legacy features and add a parallel
residual for exact NUMBER values,
selection context/type/card/effect, min/max count, remaining damage and energy
budgets, `Option.count`, stable SKILL/source identity, and complete semantic
source/target/area/slot/tool/energy binding. Where retained in raw data, add
public current/max HP, evolution stack, `appearThisTurn`, typed energy units,
Bench maximum, and once-per-turn action flags.

Use stable within-observation entity bindings. Do not blindly encode global
serials or incidental option order. Ordinal is a documented last-resort
fallback only when official libcg distinguishes otherwise identical payloads.

Connect pinned public card-catalog metadata through a zero-initialized parallel
adapter. Accept all observed `ex`/`megaEx` combinations; Mega ex takes
precedence as three Prizes, while ordinary ex is two unless a visible effect
changes it. Terminal result/reason and future transitions remain target-only.
The census may reject an unsupported branch or leave it permanently zero/inert;
implementation alone never establishes evidence or training eligibility.

The adapter consumes structured public simulator/catalog fields only through
its new additive path alongside/after the frozen Card2Vec representation. It
does not remove, retrain, re-embed, alter inputs to, or otherwise modify
Card2Vec. Text hashes remain permissible only inside their pre-existing frozen
legacy path and are never treated as exact rules or target provenance here.

## Phase C — target repairs

Freeze the requested target and target-provenance schema with the feature
schema, then populate it from selected-action public trajectories during the
30-day pass. Repair lethal-threat terminal right-censoring, Prize liability
and visible modifiers, action-utility credit through deterministic attack →
KO → Prize → promotion prompts, terminal-conversion across those chains, and
exact forced-draw/deck-out timing.

Do not invent labels for unchosen actions. Any needed counterfactual corpus is
a separate, targeted libcg state-restoration corpus with public-information
seeds and explicit provenance. A legality predictor is diagnostic only and can
never add, remove, or alter libcg's legal options.

## Required tests

Engine-backed tests cover attacks omitted for full-Bench, deck-zero draw, or
opponent-hand-zero requirements; Nullifying Zero's automatic left-to-right
resolution without target order; simultaneous-KO prompt order and double
closeout draw; ordinary, ordinary-ex, and Mega-ex Prize outcomes; and visible
Prize modifiers.

Information-set metamorphic pairs keep public state/history fixed while
changing privileged hands, deck order, and unrevealed Prizes. Sanitized hashes,
board/option features, base and candidate logits, and public-belief proposals
must be identical. The realized transition may differ; the pre-transition
decision may not depend on the hidden truth.

## Eight-checklist contract

At every actor-visible decision or forced-selection frame, emit:

1. `ko_hand_threshold`
2. `safe_spend_above_threshold`
3. `replacement_alakazam_line`
4. `unavoidable_draws_before_attack`
5. `bench_prize_exposure`
6. `immediate_disruption_outcome`
7. `unknown_prize_robust_line`
8. `terminal_before_forced_draw`

Every channel reports public factual evidence, availability or unknown status,
an unavailable reason, per-option signed evidence where applicable, its
applied gate, and final residual. Unknown or hidden evidence produces exactly
zero residual. Q3 is Bench-only. Q5 and Q6 remain trace-only with exact-zero
gates until their narrow factual predicates pass independent validation and
calibration; do not claim complete eight-channel logit coverage while either
gate is zero. Freeze this checklist-provenance schema before re-featurization
and emit its evidence records during that pass. Unsupported provenance branches
remain zero/inert after the census.

## Training and evaluation

First complete and checksum-freeze the new feature, target, and checklist-
provenance schema. Immediately scan every episode in all 30 validated UTC
partitions under it while producing the collision census, then materialize only
the revision-6-eligible acting-seat rows. Train, validation, and
evaluation manifests must be simultaneously disjoint by source, UTC day, and
group, and must bind the validated/deduplicated raw manifest SHA-256. Freeze
the backbone, existing heads, Fusion, OwnDeck routes, and matchup adapters.
After corpus validation, census, the revision-6 scope-migration receipt, and
every revision-5 lifecycle handoff receipt only,
train the new public-rule adapter, repaired heads, and explicitly eligible
bounded gates on Inzi Blackwell. Unsupported branches remain zero/inert. Do not
recollect unless the raw audit first proves the required evidence absent; never
fall back silently to 20 days. Current r274 remains healthy and active until
the clean boundary; after activation it is fully paused and cannot train or
collect concurrently. Training, validation, and evaluation rows obey the same
acting-seat setup-deck-contains-`743` predicate and preserve the acting setup-
deck multiset digest for list-variant strata.

Report three comparisons separately:

- A: candidate versus unchanged r241/r274 on the exact same new list;
- B: candidate versus r195 in r195's native operational configuration; and
- C: exploratory r195 weights with the new list, explicitly labeled a
  deck-shift confound.

BO250 means 125 seed-matched, seat-swapped pairs per reported comparison. It is
not conclusive by itself. Report paired confidence intervals, draws, action
disagreements, severe rule-error rate, per-channel coverage, and the residual
that changed each action. BO250 is forbidden until the 30-day corpus-validation
and complete collision-census receipts pass and a receipt-bound candidate has
been trained only on census-supported branches.

## Completion deliverables

Completion requires the raw-field and collision census, direct/derived field
classification, exact source changes, feature and target schema manifests,
hidden-information proof, simulator and catalog digests, unit and engine test
results, the validated/deduplicated 30-partition manifest SHA-256,
the frozen feature/target/checklist-provenance schema manifest and bypass
receipt, source/day/group-disjoint re-featurization and collision-census
receipts, per-shard and final create-only source/Inzi parity receipts,
Card2Vec bit-identity and additive-path receipts, resource-peak receipts,
paired benchmark report, explicit limits, and one recommendation: reject;
retain as trace/training aid; or request a future promotion decision. Also
require the revision-6 corpus-scope migration receipt with all-episode audit,
acting-seat inclusion/exclusion, and list-variant-strata evidence. Continue to
require the exact parent, corpus, schema-freeze, frozen-tensor, Blackwell
preflight, rollback-plan, readiness, handoff-activation, bootstrap-candidate,
Kaggle package/queue/eventual-upload, fleet inventory/package-parity/routing/
activation, and collection-shard receipts defined by `contract.json`.

Revision 5, as narrowed by revision 6, grants only those receipt-gated Inzi
training, one-shot Kaggle queue, and fleet self-play actions. It grants no
immediate action, serving
selector, production deployment, extra submission, or destructive overwrite.

## Safe resume

Read this file completely, then read
`goals/alakazam-elmo-rule-derivative/contract.json` completely. Treat reports,
configs, code, and receipts as subordinate evidence. Before the complete
revision-6 readiness set exists, continue only isolated Elmo work and revision-4
create-only staging. At readiness, follow the exact closed receipt sequence in
`contract.json`; never infer authority from prose, a partial receipt, a Kaggle
score, or the mere presence of staged bytes. Never change the production
serving selector under this goal.
