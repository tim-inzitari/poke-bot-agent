# Alakazam Elmo Rule-Derivative Goal Gateway

Schema: `poke_bot.goal_gateway/v1`

Revision: `7`

Root handoff revision: `303-TRAINING`

Status: `authoritative_for_staged_elmo_to_inzi_training_handoff`

This is the stable goal entry point for the isolated Alakazam public-rule
representation and auxiliary-head experiment. It is a separate goal from the
production Pokemon RL controller. Revision 5 authorizes a future fail-closed
handoff from Elmo preparation to Inzi Blackwell training, one checksum-exact
Kaggle queue entry, and then derivative self-play across the verified available
fleet. It grants no immediate service/runtime change and no serving-selector or
production deployment authority. Revision 6 clarifies corpus row scope only;
it does not change the revision-5 lifecycle or authorize an immediate action.

The sole typed canonical source for this experiment is
`goals/alakazam-elmo-rule-derivative/contract.json`. Implementation configs,
reports, manifests, and receipts are subordinate artifacts and must bind that
contract; none may become a second authority.

## Objective

Build and validate on Elmo an Alakazam derivative that repairs confirmed
public-rule representation collisions and honestly covers the eight
turn-checklist questions. After the frozen schema, exact 30-day corpus,
collision census, and quarantined shard parity are complete, preserve healthy
r274 work to one clean durable boundary, then use exact receipts to hand the
new bootstrap/training lineage to Inzi's Blackwell. After successful bootstrap
validation, build and durably queue exactly one checksum-exact
`first_if_allowed` Kaggle submission; after the queue receipt is accepted or
pending only on quota/spacing, begin derivative self-play across the
checksum-proven full available fleet without waiting for a Kaggle score.

The intended result is:

1. a measured collision and rules-coverage report over raw Elmo episodes;
2. the smallest versioned representation and head changes justified by that
   report;
3. a frozen-parent Inzi Blackwell candidate;
4. simulator-rule tests plus paired benchmarks against unchanged r241/r274
   and immutable r195;
5. one single-use checksum-exact Kaggle queue handoff; and
6. receipt-backed derivative self-play across the full available fleet.

## Source and provenance

The current owner source is
`/Users/tsinzitari/.codex/attachments/8100a736-c437-41f2-b559-97f78d2e2f78/pasted-text.txt`
with
`sha256:0f440fc71043b4352e6401a3187c9d582c1c5614d76e186095e0eef51017af6f`.

Root revision 298 and its attachment
`/Users/tsinzitari/.codex/attachments/7b1d4464-b2f6-4fcc-8a6e-52abc35e3aaf/pasted-text.txt`
(`sha256:d3f06071663dde2ae7012da72b407b410c7facd06d09ab723cad05af44ddb2cb`)
remain immutable historical provenance. Root revision 302 handed experiment
design to this gateway. Root revision 303 and
`state/alakazam-new-list-direct-policy-r241.json` now own only the exact
production training-lineage transition, service boundary, Kaggle queue, and
fleet activation; they do not duplicate representation or target semantics.
Revision 6 is an explicit owner clarification recorded on 2026-08-12 without
a new attachment; the two attachment identities above remain provenance, not
the source of this narrower row-eligibility rule.

## Dedicated decision ledger

| Revision | Recorded at (UTC) | Decision |
|---|---|---|
| 1 | 2026-08-12 | Establish the separate, isolated Elmo-only goal, public-information contract, zero-gated frozen-backbone derivative, Phase A--C workflow, eight-channel trace contract, and three paired evaluation cohorts. |
| 2 | 2026-08-12 | Make the latest complete contiguous raw archive window, 2026-07-13 through 2026-08-11 UTC inclusive, mandatory for Phase A and re-featurization: exactly 30 distinct UTC partitions, one validated and deduplicated checksum-bound manifest, no silent 20-day/subset/adjacent-window fallback, no recollection without proved absence, and source/day/group-disjoint splits. Permit isolated jobs to use all Elmo CPU/GPU under a hard 96-GiB experiment-RAM ceiling, serialize memory-heavy phases, allow normal existing TrueNAS/ZFS L2ARC caching, prohibit ZFS/ARC/L2ARC tuning and managed-service changes, and require resource-peak receipts. |
| 3 | 2026-08-12 | Implement the requested isolated versioned representation, target, and checklist-provenance paths and freeze their feature schema first; then immediately validate and re-featurize the exact revision-2 30-day corpus. The collision census/report may be produced during or from that same pass rather than blocking zero-inert implementation. Corpus validation and census evidence still gate every nonzero activation, candidate-training step, and BO250. Unsupported branches remain zero/inert and may be rejected after census. |
| 4 | 2026-08-12 | After each immutable re-featurization/census shard is complete, at most 96 MiB, and individually schema/SHA/size validated, transfer it concurrently through bounded lanes into one quarantined create-only Inzi staging directory. Use full-content-addressed names, bind every object to the frozen schema and 30-day manifest, verify remote SHA and size, skip exact matches, refuse conflicts, and finish with a source/remote parity receipt. This is byte-staging authority only: Inzi may not load or use the artifacts. Preserve Card2Vec completely—learned embeddings, inputs, text-derived knowledge, checkpoint tensors, and behavior remain frozen/untouched. The new structured simulator residual is additive alongside/after Card2Vec, never a replacement; legacy text hashes retain only their existing frozen role and cannot provide exact mechanics authority to the new residual. |
| 5 | 2026-08-12 | After Elmo implementation/schema freeze, exact 30-day re-featurization/census, branch adjudication, and verified shard staging, supersede the current Inzi r274 training lineage at one clean receipt-backed boundary. Let current healthy r274 work seal first; fully pause only its trainer and r274 submission-boundary services, preserve the shared Kaggle queue service and all immutable rollback artifacts, make staged shards training-eligible atomically, and bootstrap/train the derivative on Inzi's `cuda:1` RTX PRO 5000 Blackwell from the exact sealed r274 parent with Card2Vec/backbone/existing heads/Fusion/OwnDeck/Matchup Adapters frozen. After validation, build and durably queue one checksum-exact `first_if_allowed` Kaggle package on the exact new list; then begin derivative self-play across the fully inventoried/checksum-matched available fleet without waiting for score. No runtime/service change occurs while recording this revision; no serving selector is authorized. |
| 6 | 2026-08-12 | Scan every episode in the exact 30-day raw audit, but materialize re-featurized feature/target/checklist rows and training/validation/evaluation rows only for a decision's acting seat when that same seat's recorded setup deck contains card ID 743. Literal membership is the complete predicate: an exact pilot-list match is not required and there is no tech-inclusion exception. Exclude decisions made by a non-qualifying opponent seat even when the other seat qualifies; include each seat independently when both qualify. Preserve the acting setup-deck canonical multiset digest on every included row and in list-variant strata. Accept unchanged revision-5 raw manifests, frozen schema bytes, and correctly scoped outputs through a checksum-bound revision-6 migration receipt; do not recompute solely because the goal/contract revision changed. No runtime/service action is authorized. |
| 7 | 2026-08-12 | Warm-start the derivative bootstrap from immutable r195 for every exact-name/shape-compatible legacy backbone and policy tensor because r195 is the strongest Alakazam weight parent. Fill architecture additions absent from r195—newer auxiliary heads, Fusion/OwnDeck additions, and matchup adapters—from the exact clean-boundary r274 checkpoint. Initialize only the genuinely new rule-residual/repaired-head parameters separately. Require a per-tensor source map, reject ambiguous overlaps or shape drift, keep every inherited tensor frozen during initial bootstrap, and preserve both checkpoints unchanged. r274 remains the same-architecture evaluation baseline and rollback source; r195 is now the primary compatible-weight warm-start source rather than only a benchmark. Accept unchanged revision-6 corpus/schema/census artifacts without recomputation. No immediate runtime/service action is authorized. |

## Authority and ground truth

- Phase A scans every episode in the latest complete contiguous 30-day raw
  expert archive on Elmo, exactly 2026-07-13 through 2026-08-11 UTC inclusive:
  exactly 30 distinct UTC day partitions bound by one validated, deduplicated
  manifest SHA-256. No deck, seat, archetype, or row-eligibility prefilter may
  narrow this raw audit. A 20-day, adjacent, or other subset/window is not a
  fallback. Recollection is forbidden unless the manifest/audit first proves
  the needed source evidence absent.
- The exact pinned official libcg binary, identified by SHA-256, is gameplay
  ground truth. Its emitted legal option list is authoritative.
- Official paper rules or prose are secondary when they differ from libcg.
- r195 is the best operational reference, but it has an older architecture and
  a somewhat different native deck list.
- Unchanged r241/r274 is the fair same-architecture baseline for new heads and
  matchup adapters.
- “Next Alakazam line” means a replacement attacker line on the Bench. The
  current Active Alakazam never satisfies that channel.

## Hard boundaries

- Until revision 6's complete readiness, corpus-scope migration, and activation
  receipts exist,
  revision 4's exact quarantined create-only shard transfer remains the only
  permitted Inzi action. Staged shards may not be loaded or used early. This
  metadata update itself performs no Inzi file, runtime, worker, service,
  checkpoint, queue, submission, or collection action.
- Preserve current healthy r274 training until a clean durable boundary. Never
  pause mid-collection, mid-shard, mid-optimizer, mid-adapter, mid-checkpoint,
  or mid-commit. At handoff, fully pause only
  `pokebot-alakazam-r274-rl.service` and
  `pokebot-alakazam-r274-rl-submission-boundaries.service` through user systemd;
  never signal processes directly. The shared Kaggle queue service remains
  untouched.
- Preserve parent checkpoints and current feature artifacts immutably.
- The separately versioned derivative is zero-gated. With the new path off, it
  bypasses completely and reproduces baseline logits bit-for-bit.
- Preserve the complete existing Card2Vec system: learned embeddings, current
  inputs, text-derived knowledge, checkpoint tensors, and runtime behavior are
  frozen and untouched. The structured simulator residual is additive
  alongside/after Card2Vec, never a replacement or reinterpretation. Existing
  text hashes may remain inside the frozen legacy/Card2Vec path, but they are
  not exact mechanics authority and cannot generate mechanics fields or labels
  for the new residual.
- Policy, value, Fusion, checklist logic, adapters, and search use only the
  acting player's public information set.
- Exact opponent-hand identities, opponent deck order, and unrevealed Prize
  identities are never runtime inputs. Privileged fields are separately typed
  target-only labels and cannot choose teacher/search/policy actions.
- Do not infer unsupported mechanics from text hashes. Missing or malformed
  evidence is reported as unavailable rather than guessed.
- Isolated jobs may use all available Elmo CPU and GPU capacity, but aggregate
  experiment RAM has a hard ceiling of 96 GiB. Only one memory-heavy phase may
  run at a time.
- Normal existing TrueNAS/ZFS L2ARC caching is allowed. Do not tune, resize,
  flush, replace, or otherwise reconfigure ZFS, ARC, or L2ARC, and do not
  create, edit, stop, restart, reload, or replace managed services for this
  experiment.
- Every isolated-job receipt reports measured resource peaks, including its
  RAM accounting method and peak, CPU/process peak, and per-device GPU memory
  and utilization peaks where applicable.

## Revision 3 execution order

1. Implement the requested isolated, separately versioned representation,
   target, and checklist-provenance changes. Every new feature branch is
   default-zero and inert; this step grants no training or action authority.
2. Freeze the complete feature/target/provenance schema and issue a
   checksum-bound schema manifest and zero-bypass receipt.
3. Immediately validate, deduplicate, and re-featurize the exact 2026-07-13
   through 2026-08-11 UTC corpus under that frozen schema. Generate the full
   collision census during this pass or deterministically from its immutable
   outputs; a separate pre-implementation census is not required.
4. Use the completed corpus-validation and census receipts to adjudicate each
   branch. A branch without support stays exact-zero/inert and may be rejected;
   it cannot silently become trainable or active.

No derivative branch may receive nonzero activation and no candidate training
or BO250 may begin until the exact 30-day corpus and full census validate.

## Revision 4 create-only transfer staging

As soon as one re-featurization/census shard is complete and immutable, is no
larger than 96 MiB (100,663,296 bytes), and passes its individual schema,
SHA-256, and exact-size validation, it may transfer in parallel with the
remaining Elmo pass. The destination is the quarantined, non-runtime,
create-only Inzi directory
`/home/inzi/poke-bot-agent/outputs/quarantine/alakazam-elmo-rule-derivative/g4-refeaturized-census-shards`.

Each object uses its complete lowercase SHA-256 in its filename, binds the
frozen feature/target/checklist-provenance schema digests and validated 30-day
raw-manifest SHA-256, and is published immutably. Transfer uses at most four
parallel lanes; every individual object remains under the approximately
100-MB gateway through the stricter 96-MiB limit. A remote object is complete
only after post-transfer SHA-256 and exact-size verification.

The copy path is resumable and idempotent: a byte-exact verified destination is
skipped, an incomplete object may resume through a private partial name before
atomic create-only publication, and any existing conflicting final name or
content is refused without overwrite, delete, rename, or replacement. After
all source shards complete, a final receipt proves source/remote manifest,
object-count, filename, SHA-256, and size parity.

This grants only directory creation and create-only transfer/staging authority.
The Inzi artifacts remain quarantined and training-ineligible. No Inzi load,
decode, import, mmap, cache warm, training, collection, selector, runtime,
worker, service, checkpoint, package, submission, propagation, or activation
is authorized. No managed-service creation or change is authorized.

## Revision 5 receipt-backed Inzi handoff

Revision 4 staging receipts alone never authorize use. After the frozen schema,
exact 30-partition corpus, complete census/branch adjudication, source/remote
parity, exact r274 parent, frozen-tensor identity, Blackwell preflight, and
rollback plan all pass, seal one readiness receipt. Let every current r274
collection/shard, optimizer/adapter step, checkpoint, and commit finish first.
At the next idle boundary, pause exactly the r274 trainer and r274
submission-boundary units through user systemd, seal their checkpoint and
optimizer state, leave the shared Kaggle queue service unchanged, and issue the
handoff activation receipt. Only that receipt atomically makes the staged
shards training-eligible and authorizes the new managed derivative bootstrap.

The bootstrap is a checksum-bound composite warm start. Immutable r195 supplies
every exact-name/shape-compatible legacy backbone and policy tensor. The last
immutable safety-approved r274 checkpoint sealed at the boundary supplies only
architecture additions absent from r195, including newer heads and matchup
adapters. A per-tensor source map must cover every inherited tensor exactly
once and reject ambiguous overlaps or shape drift. “Overwrite” means the derivative becomes the sole active Alakazam
training/collection lineage. It never means modifying or deleting r274 bytes.
All r274 checkpoints, optimizer state, registries, service definitions, and
receipts remain checksum-bound rollback evidence. Card2Vec, the neural
backbone, existing heads, Fusion, OwnDeck routes, and Matchup Adapters remain
bit-identical and absent from the new optimizer; only the new adapter, repaired
heads, and census-supported gates train on Inzi `cuda:1` (RTX PRO 5000
Blackwell). Any identity, resource, finiteness, frozen-tensor, layer-off, load,
or launch failure fails closed into the receipt-backed rollback plan.

After the Blackwell bootstrap candidate passes engine, information-set,
frozen-tensor, and package validation, build exactly one package using the
exact new-list deck and the bound runtime, simulator, catalog, schema,
checklist, router, and adapter identities. Durably enqueue its one single-use
`first_if_allowed` authorization in the existing shared Kaggle queue. Quota or
spacing may leave it pending; upload failure or delay never changes, deletes,
or falsifies the checkpoint, and no result is called submitted without an
upload receipt.

Once the durable queue receipt says accepted or pending only on quota/spacing,
self-play may start without waiting for upload or score. Inventory every known
candidate host, record availability/exclusion evidence, checksum the exact
package on every eligible host, and bind explicit per-device worker routing and
resource caps. Inzi Blackwell remains the sole learner/optimizer. Use globally
unique collection/game/lease identities, append-only shards, and exact
duplicate suppression so no game is counted twice. No old r274 training or
collection may run concurrently. Fleet inventory, package parity, routing,
activation, and per-shard receipts are mandatory; an unreceipted host drop,
device fallback, service change, or resource-cap weakening fails closed.

Exact typed receipt schemas and their closed top-level field inventories are
owned by `contract.json`. Receipts contain no self-referential digest field;
each receipt's exact bytes are SHA-256-bound by its consuming receipt.

## Phase A — raw-corpus audit

First validate and deduplicate one manifest covering the full existing Elmo raw
expert corpus from 2026-07-13 through 2026-08-11 UTC inclusive. It must
enumerate exactly those 30 distinct contiguous UTC partitions and carry its
SHA-256. Missing, duplicated, overlapping, non-UTC, or adjacent-window
partitions fail closed; no code may silently substitute the older 20-day
window or another subset/window. A recollection request is ineligible until
this audit proves the required evidence absent. The audit enumerates and scans
every raw episode before any deck or acting-seat predicate is applied.

The zero-inert implementation and frozen schema now precede this full audit.
Inventory the raw Elmo schema across all 30 validated partitions during the
same re-featurization pass or deterministically from its immutable outputs.
Classify every candidate field as a direct public libcg field, immutable public
catalog metadata, transition-derived target, fresh-simulator target, or hidden
information. The completed audit, not pre-audit assumptions, determines which
frozen branches may become training-eligible.

For every actor-visible selection frame, census `SelectContext`,
`effect`/`contextCard`, the complete raw option, current feature signature,
selected option, and adjacent public successor when available. Measure all
NUMBER values, especially 4 versus 5; same-card SKILL options with distinct
serial/source; duplicate ATTACK payloads; and distinctions in count, source,
target, effect, and remaining budget. Report frequency, selected-action
involvement, matchup distribution, and deck distribution.

A collision is actionable only when publicly distinguishable states or options
have different simulator semantics. Hidden-state equivalence and harmless
permutation equivalence are not bugs.

## Revision 6 acting-seat row scope

The raw audit and its field/collision census scan every episode in all 30
validated partitions. Row materialization is narrower. For each actor-visible
decision, inspect only that decision's acting seat and that same seat's exact
recorded setup deck. Materialize the re-featurized feature, target, and
checklist-provenance row if and only if the setup deck contains at least one
card ID `743` (Alakazam).

This literal membership test is the complete eligibility predicate. Do not
require the exact pilot list, its digest, a deck/archetype label, a minimum
Alakazam-line count, or any other core-card signature. There is no
tech-inclusion exception: a deck containing `743` qualifies even if another
classifier would call that copy a tech. If only one seat qualifies, exclude
every decision made by the other, non-Alakazam seat. If both seats qualify,
include each seat's own acting decisions independently. If neither qualifies,
the episode still contributes to the all-episode raw audit/census but produces
no re-featurized rows. Missing or malformed setup-deck evidence is never
guessed; the affected seat's rows are excluded with an explicit reason and
count.

Every included row preserves `acting_deck_multiset_sha256`, computed from the
complete recorded acting-seat setup-deck card-ID multiset using the existing
canonical sorted-array JSON encoding. Train, validation, and evaluation
manifests retain that digest and report per-digest row/game counts as list-
variant strata. The digest is provenance/stratification, not an exact-pilot
eligibility gate and not a new hidden opponent-deck runtime input.

Revision 6 does not change frozen feature, target, checklist-provenance,
catalog, simulator, Card2Vec, or zero-bypass schema bytes. The exact revision-5
30-day raw manifest remains valid when it already enumerates and scans every
episode. Revision-5 schema/freeze/parity receipts and correctly filtered
re-featurized shards remain immutable predecessor evidence and may be accepted
without recomputation after a revision-6 scope-migration receipt binds their
digests and proves this row predicate, seat exclusion, and per-list strata.
Incorrectly scoped outputs must be deterministically filtered or rematerialized,
but the unchanged raw audit and frozen schemas are not rebuilt merely for the
revision bump.

## Phase B — minimal versioned representation repair

Before the 30-day pass, implement the requested repairs supported by this
contract or the official API as default-zero, inert, separately versioned
branches and freeze their schema. Preserve legacy features and add a parallel
residual for exact NUMBER values,
selection context/type/card/effect, min/max count, remaining damage and energy
budgets, `Option.count`, stable SKILL/source identity, and complete semantic
source/target/area/slot/tool/energy binding. Where retained in raw data, add
public current/max HP, evolution stack, `appearThisTurn`, typed energy units,
Bench maximum, and once-per-turn action flags.

Use stable within-observation entity bindings. Do not blindly encode global
serials or incidental option order. Ordinal is a documented last-resort
fallback only when official libcg distinguishes otherwise identical payloads.

Connect pinned public card-catalog metadata through a zero-initialized parallel
adapter. Accept all observed `ex`/`megaEx` combinations; Mega ex takes
precedence as three Prizes, while ordinary ex is two unless a visible effect
changes it. Terminal result/reason and future transitions remain target-only.
The census may reject an unsupported branch or leave it permanently zero/inert;
implementation alone never establishes evidence or training eligibility.

The adapter consumes structured public simulator/catalog fields only through
its new additive path alongside/after the frozen Card2Vec representation. It
does not remove, retrain, re-embed, alter inputs to, or otherwise modify
Card2Vec. Text hashes remain permissible only inside their pre-existing frozen
legacy path and are never treated as exact rules or target provenance here.

## Phase C — target repairs

Freeze the requested target and target-provenance schema with the feature
schema, then populate it from selected-action public trajectories during the
30-day pass. Repair lethal-threat terminal right-censoring, Prize liability
and visible modifiers, action-utility credit through deterministic attack →
KO → Prize → promotion prompts, terminal-conversion across those chains, and
exact forced-draw/deck-out timing.

Do not invent labels for unchosen actions. Any needed counterfactual corpus is
a separate, targeted libcg state-restoration corpus with public-information
seeds and explicit provenance. A legality predictor is diagnostic only and can
never add, remove, or alter libcg's legal options.

## Required tests

Engine-backed tests cover attacks omitted for full-Bench, deck-zero draw, or
opponent-hand-zero requirements; Nullifying Zero's automatic left-to-right
resolution without target order; simultaneous-KO prompt order and double
closeout draw; ordinary, ordinary-ex, and Mega-ex Prize outcomes; and visible
Prize modifiers.

Information-set metamorphic pairs keep public state/history fixed while
changing privileged hands, deck order, and unrevealed Prizes. Sanitized hashes,
board/option features, base and candidate logits, and public-belief proposals
must be identical. The realized transition may differ; the pre-transition
decision may not depend on the hidden truth.

## Eight-checklist contract

At every actor-visible decision or forced-selection frame, emit:

1. `ko_hand_threshold`
2. `safe_spend_above_threshold`
3. `replacement_alakazam_line`
4. `unavoidable_draws_before_attack`
5. `bench_prize_exposure`
6. `immediate_disruption_outcome`
7. `unknown_prize_robust_line`
8. `terminal_before_forced_draw`

Every channel reports public factual evidence, availability or unknown status,
an unavailable reason, per-option signed evidence where applicable, its
applied gate, and final residual. Unknown or hidden evidence produces exactly
zero residual. Q3 is Bench-only. Q5 and Q6 remain trace-only with exact-zero
gates until their narrow factual predicates pass independent validation and
calibration; do not claim complete eight-channel logit coverage while either
gate is zero. Freeze this checklist-provenance schema before re-featurization
and emit its evidence records during that pass. Unsupported provenance branches
remain zero/inert after the census.

## Training and evaluation

First complete and checksum-freeze the new feature, target, and checklist-
provenance schema. Immediately scan every episode in all 30 validated UTC
partitions under it while producing the collision census, then materialize only
the revision-6-eligible acting-seat rows. Train, validation, and
evaluation manifests must be simultaneously disjoint by source, UTC day, and
group, and must bind the validated/deduplicated raw manifest SHA-256. Freeze
the backbone, existing heads, Fusion, OwnDeck routes, and matchup adapters.
After corpus validation, census, the revision-6 scope-migration receipt, and
every revision-5 lifecycle handoff receipt only,
train the new public-rule adapter, repaired heads, and explicitly eligible
bounded gates on Inzi Blackwell. Unsupported branches remain zero/inert. Do not
recollect unless the raw audit first proves the required evidence absent; never
fall back silently to 20 days. Current r274 remains healthy and active until
the clean boundary; after activation it is fully paused and cannot train or
collect concurrently. Training, validation, and evaluation rows obey the same
acting-seat setup-deck-contains-`743` predicate and preserve the acting setup-
deck multiset digest for list-variant strata.

Report three comparisons separately:

- A: candidate versus unchanged r241/r274 on the exact same new list;
- B: candidate versus r195 in r195's native operational configuration; and
- C: exploratory r195 weights with the new list, explicitly labeled a
  deck-shift confound.

BO250 means 125 seed-matched, seat-swapped pairs per reported comparison. It is
not conclusive by itself. Report paired confidence intervals, draws, action
disagreements, severe rule-error rate, per-channel coverage, and the residual
that changed each action. BO250 is forbidden until the 30-day corpus-validation
and complete collision-census receipts pass and a receipt-bound candidate has
been trained only on census-supported branches.

## Completion deliverables

Completion requires the raw-field and collision census, direct/derived field
classification, exact source changes, feature and target schema manifests,
hidden-information proof, simulator and catalog digests, unit and engine test
results, the validated/deduplicated 30-partition manifest SHA-256,
the frozen feature/target/checklist-provenance schema manifest and bypass
receipt, source/day/group-disjoint re-featurization and collision-census
receipts, per-shard and final create-only source/Inzi parity receipts,
Card2Vec bit-identity and additive-path receipts, resource-peak receipts,
paired benchmark report, explicit limits, and one recommendation: reject;
retain as trace/training aid; or request a future promotion decision. Also
require the revision-6 corpus-scope migration receipt with all-episode audit,
acting-seat inclusion/exclusion, and list-variant-strata evidence. Continue to
require the exact parent, corpus, schema-freeze, frozen-tensor, Blackwell
preflight, rollback-plan, readiness, handoff-activation, bootstrap-candidate,
Kaggle package/queue/eventual-upload, fleet inventory/package-parity/routing/
activation, and collection-shard receipts defined by `contract.json`.

Revision 5, as narrowed by revision 6, grants only those receipt-gated Inzi
training, one-shot Kaggle queue, and fleet self-play actions. It grants no
immediate action, serving
selector, production deployment, extra submission, or destructive overwrite.

## Safe resume

Read this file completely, then read
`goals/alakazam-elmo-rule-derivative/contract.json` completely. Treat reports,
configs, code, and receipts as subordinate evidence. Before the complete
revision-6 readiness set exists, continue only isolated Elmo work and revision-4
create-only staging. At readiness, follow the exact closed receipt sequence in
`contract.json`; never infer authority from prose, a partial receipt, a Kaggle
score, or the mere presence of staged bytes. Never change the production
serving selector under this goal.
